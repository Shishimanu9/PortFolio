/**
 * Services — data source.
 *
 * Services.jsx maps over this array and renders a <ServiceCard /> per
 * entry. Intended categories per the brief (fill in title/description
 * yourself): Machine Learning, Data Analysis, Data Cleaning,
 * LLM Applications, Computer Vision, Geospatial AI.
 *
 * Shape consumed by ServiceCard:
 *
 * {
 *   id: string,
 *   icon: string,        // react-icons key resolved in ServiceCard, e.g. "FiCpu"
 *   title: string,
 *   description: string,
 * }
 */

export const services = [
  // { id: "", icon: "", title: "", description: "" },
];
