/**
 * Skills — data source.
 *
 * Skills.jsx maps over grouped categories. Grouping keeps the UI from
 * becoming an undifferentiated tag cloud (recruiters and clients scan
 * by category: languages vs frameworks vs tools vs domains).
 *
 * Shape:
 *
 * {
 *   category: string,     // e.g. "Languages", "ML / DL", "MLOps & Tools"
 *   items: string[],
 * }
 */

export const skills = [
  // { category: "", items: [] },
];
