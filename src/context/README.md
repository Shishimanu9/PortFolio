Reserved for future global state (e.g. a theme toggle, or an
active-project modal context) if the app grows past what
prop/hook composition can handle cleanly. Empty for now by design —
this is a single-page, mostly-static site with no shared state yet.
